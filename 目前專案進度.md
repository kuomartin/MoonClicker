ReLC 專案進度概覽（更新至 2026-05-27）

本文件整理 ReLC 專案的整體進度，對應 ARCHITECTURE.md 的設計與模組實作狀態，方便團隊在不同階段快速對齊目標與風險。

1. 概覽
- ReLC 是一個 Android 平台的遠端 Lua 控制與自動化工具，涵蓋虛擬顯示、輸入注入、腳本執行與圖像辨識等核心能力。
- 目前實作以模組化設計為主，核心能力包含虛擬顯示管理、流式編碼/分發、Lua 引擎與 UI 框架的橋接。

2. 進度摘要（對應 P1-P4 的階段劃分）
- P1（核心權限與顯示）
  - 完成內容：擴充 AIDL、實作 createVirtualDisplay 與 launchInDisplay、修復 InputManager 的 displayId 行為。
  - 現況：穩定運作於多版本 Android，具備基本顯示建構與啟動能力。
- P2（顯示與預覽 / Streaming）
  - 完成內容：VirtualDisplay Controller、DisplaySink 抽象與實作（NoOpSink、H264EncoderSink、DirectSink 的框架）
  - 進展：GLES Distributor（原生分發器）已實作，OpenGL 渲染管線可支援多客戶端畫面分發，但 H264 編碼與 LocalSocket 的整合仍在推進中。
- P3（腳本引擎與 UI 桥接）
  - 完成內容：Lua 原生引擎框架、LuaNative 與 UI 框架橋接、ScriptEngine/ScriptRunner 的核心流程。
  - 現況：Lua API 正在穩定豐富，Lua 與 Compose UI 的動態節點管理功能已能使用。
- P4（視覺辨識與 OCR）
  - 完成內容：VisionEngine 框架的基本設計已在原生端完成，OpenCV 模板比對的支援已在虛擬顯示流程中留存。
  - 現況：OCR（ML Kit）與更高階的視覺模型尚未全面整合，屬於待辦項。

3. 模組狀態清單
- 模組 | 功能 | 狀態
- Shizuku ShizukuService / RelcV2Service | AIDL 介面、虛擬顯示與注入 | 完成
- VirtualDisplayController + DisplaySink | 生命週期管理、熱替換輸出 | 完成 / 部分實作字形伺
- H264EncoderSink + LocalSocketServer/Client | 編碼與傳輸管道 | 進行中
- VideoPlayerUI / FrameGrabber | 本地播放與幀抓取 | 進行中
- Script Engine (Lua 原生引擎) | Hot loop 與 API 暴露 | 核心完成
- LuaNative 與 LuaUiManager | UI 桥接與動態 UI | 完成
- VisionEngine (OpenCV) | 模板比對 | 框架完成，API 擴展中
- OCR（ML Kit） | 文字辨識 | 待辦

4. 風險與挑戰
- Lua API 的完善度：持續增加 vision 相關 API，避免 API 過於跳脫與不穩定。
- 性能與穩定性：H264 視訊串流與分發在高畫質、低延遲場景下可能遇到資源競爭，需要動態調整解析度與編碼設定。
- 跨版本相容性：Android 版本差異導致虛擬顯示旗標與系統服務行為不同，需要廣覆測試清單。
- 模型與模組下載：OCR/OpenCV 模型與 ML Kit 模型的動態下載需實作快取與容錯機制。

5. 下一步建議
- 梳理並完成 P2 的 H264 流/LocalSocket 的傳輸層整合與測試，確保多客戶端一致性。
- 擴展 vision.* API，並整合模板比對與簡易的視覺工作流至 Lua 腳本中。
- 補強 ML Kit OCR 的動態下載與版本管理機制。
- 完成一致的單元測試/整合測試計畫，覆蓋虛擬顯示建立、啟動 App、觸控輸入與視覺辨識結合。

6. 測試與驗證清單
- [ ] 在至少三個不同 Android 設備上驗證虛擬顯示器建立與銷毀穩定性
- [ ] 驗證 display.create 與 display.launchInDisplay 的流程在 API 34 及以上版本的兼容性
- [ ] 多點觸控注入在不同 displayId 的一致性測試
- [ ] H264 影音分發的延遲與穩定性測試（各 Sink 路徑）
- [ ] OpenCV 模板比對準確性測試與效能測試

如要，我可以把這個草案直接提交為正式的進度文檔，並根據你的回饋做修訂。
