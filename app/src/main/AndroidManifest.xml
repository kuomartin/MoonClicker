<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- targetSdk 37（Android 17）起，接受區網的 inbound TCP 連線需要這個 runtime permission，
         否則連線會卡在 ESTABLISHED 永遠讀不到資料。見 WorkbenchServer 的 workbench module。 -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />

    <!-- QR code（#56）在切換 WiFi 網路時要跟著更新位址，靠 ConnectivityManager 的網路變動
         callback（一般權限，不需要使用者授權）。 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Package visibility queries for Android 11+ -->
    <queries>
        <!-- Query for Shizuku app -->
        <package android:name="moe.shizuku.privileged.api" />

        <!-- Query for Play Store market intent -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>

        <!-- Query for web browser intent (backup for Play Store) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
    <application
        android:name=".RelcApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReLC"
    >
        <activity
            android:name=".ui.displaydetail.FullscreenDisplayActivity"
            android:exported="false"
            android:theme="@style/Theme.ReLC.Fullscreen"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
        />
        <activity
            android:name=".RelcActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ReLC"
            android:windowSoftInputMode="adjustResize"
        >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Shizuku provider -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
        />
        <!-- Handles the "停止所有" action on the script status notification. -->
        <receiver
            android:name=".notification.ScriptNotificationReceiver"
            android:exported="false"
        />
        <service
            android:name=".workbench.WorkbenchService"
            android:foregroundServiceType="specialUse"
            android:exported="false"
        >
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="script-workbench-wifi-server"
            />
        </service>
        <!-- android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" is to protect this provider from accessing by normal apps -->
    </application>

</manifest>
